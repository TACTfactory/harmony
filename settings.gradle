//include all project in vendor folder (check if build.gradle file exists)
include new File('./vendor').listFiles().findAll { 
     it.isDirectory() && new File(it.getAbsolutePath() + "/build.gradle").exists()
    }.collect { 
        'vendor:' + it.getName()
    }.toArray(new java.lang.String[0])